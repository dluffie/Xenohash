// server.js
const app = require('./app');

// The app is already started in app.js, so this file just exports the app
// This structure allows for easier testing if needed
console.log('Xenohash server starting...');
